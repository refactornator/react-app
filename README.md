# IMDB Movie Searcher
## A React/Reflux ES6 example app that builds using WebPack.

Built using IMDB's public data dump, cleaned up in Google Refine and can be found in the data folder. Poster images were downloaded using the download-posters.js script.

### Getting Started
`npm install && npm start`
